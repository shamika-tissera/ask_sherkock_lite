# from ultralytics import YOLO

# model = YOLO('yolov8m.pt')

# def count_people(frame):
#     results = model(frame)
    
#     names = model.names
#     person_id = list(names)[list(names.values()).index('person')]
#     return results[0].boxes.cls.tolist().count(person_id)

    
